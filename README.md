# MP4 to MP3 Converter

Convert .mp4 files to .mp3 using Python and moviepy.

## Usage
```bash
python mp4_to_mp3.py input.mp4

---

### 4. **Stage, Commit, and Push**
In Git Bash:

```bash
git add .
git commit -m "Initial commit: MP4 to MP3 script"
git push origin main  # or use `master` if your default branch is master
